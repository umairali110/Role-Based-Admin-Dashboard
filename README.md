# Role-Based-Admin-Dashboard
A full-stack Role-Based Admin Dashboard system built using NestJS (backend) and Next.js (frontend). It includes JWT authentication, role-based authorization (Admin/User), employee management, and secure API access with protected routes
